import {auth,db} from './firebase-config.js';import{onAuthStateChanged,signOut}from 'https://www.gstatic.com/firebasejs/10.8.1/firebase-auth.js';import{doc,getDoc,setDoc,collection,getDocs,updateDoc,query,where,serverTimestamp,Timestamp}from 'https://www.gstatic.com/firebasejs/10.8.1/firebase-firestore.js';const ADMIN_EMAIL='domingosferrazfonseca283@gmail.com',$=id=>document.getElementById(id);onAuthStateChanged(auth,async user=>{if(!user)return location.href='login.html';const s=await getDoc(doc(db,'usuarios',user.uid)),p=s.exists()?s.data():{};if(p.tipo!=='admin'||(user.email||'').toLowerCase()!==ADMIN_EMAIL){alert('Acesso exclusivo do administrador.');return location.href=p.tipo==='vendedor'?'vendedor.html':'index.html';}$('admin-info').textContent='Administrador único: '+ADMIN_EMAIL;await carregarConfig();await Promise.all([carregarVendedores(),carregarProdutos(),carregarPedidosSubscricao()]);});$('btn-logout').onclick=()=>signOut(auth);async function carregarConfig(){const s=await getDoc(doc(db,'configuracao','subscricao'));if(s.exists()){$('sub-plano').value=s.data().plano||'mensal';$('sub-valor').value=s.data().valor||'';$('sub-link').value=s.data().linkPagamento||'';}}$('btn-sub').onclick=async()=>{const valor=Number($('sub-valor').value),link=$('sub-link').value.trim();if(!valor||valor<=0)return alert('Informe um valor válido.');if(link&&!/^https:\/\//i.test(link))return alert('O link deve começar por https://');await setDoc(doc(db,'configuracao','subscricao'),{plano:$('sub-plano').value,valor,linkPagamento:link,atualizadoEm:serverTimestamp()},{merge:true});alert('Configuração guardada.');};async function carregarVendedores(){const s=await getDocs(query(collection(db,'usuarios'),where('tipo','==','vendedor'))),vs=s.docs.map(d=>({id:d.id,...d.data()}));$('total-vendedores').textContent=vs.length;$('total-ativos').textContent=vs.filter(v=>v.subscricaoAtiva===true&&v.estadoConta==='ativo').length;$('vendedores-list').innerHTML=vs.map(v=>`<div class="cart-item"><div><strong>${esc(v.nomeLoja||'Sem nome')}</strong><br>${esc(v.email||'')} · ${esc(v.estadoConta||'pendente')} · subscrição ${v.subscricaoAtiva?'ativa':'inativa'}</div><div><button class="btn" onclick="conta('${v.id}',${v.estadoConta==='ativo'})">${v.estadoConta==='ativo'?'Bloquear':'Aprovar'}</button> <button class="btn" onclick="sub('${v.id}',${v.subscricaoAtiva===true})">${v.subscricaoAtiva?'Retirar':'Conceder'} subscrição</button></div></div>`).join('')||'<p>Nenhum vendedor.</p>';}
async function carregarProdutos(){const s=await getDocs(collection(db,'produtos')),ps=s.docs.map(d=>({id:d.id,...d.data()}));$('total-produtos').textContent=ps.length;$('produtos-list').innerHTML=ps.map(p=>`<div class="cart-item"><div><strong>${esc(p.nome||'Produto')}</strong><br>${Number(p.preco||0).toLocaleString('pt-AO')} Kz · stock ${Math.max(0,Math.floor(Number(p.stock)||0))} · ${p.ativo?'publicado':'pendente'}</div><button class="btn" onclick="produto('${p.id}',${p.ativo===true})">${p.ativo?'Ocultar':'Aprovar'}</button></div>`).join('')||'<p>Nenhum produto.</p>';}
async function carregarPedidosSubscricao(){const s=await getDocs(collection(db,'pedidosSubscricao')),ps=s.docs.map(d=>({id:d.id,...d.data()}));$('pedidos-sub-list').innerHTML=ps.map(p=>`<div class="cart-item"><div><strong>${esc(p.email||p.vendedorId)}</strong><br>${esc(p.plano||'mensal')} · ${Number(p.valor||0).toLocaleString('pt-AO')} Kz · ${esc(p.estado||'aguardando_pagamento')}</div>${p.estado==='pago'?'<strong>Ativo</strong>':`<button class="btn" onclick="pagar('${p.id}','${p.vendedorId}','${p.plano||'mensal'}')">Confirmar</button>`}</div>`).join('')||'<p>Nenhum pedido.</p>';}
window.conta=async(id,ativa)=>{await updateDoc(doc(db,'usuarios',id),{estadoConta:ativa?'bloqueado':'ativo',atualizadoEm:serverTimestamp()});carregarVendedores()};window.sub=async(id,ativa)=>{await updateDoc(doc(db,'usuarios',id),{subscricaoAtiva:!ativa,atualizadoEm:serverTimestamp()});carregarVendedores()};window.produto=async(id,ativo)=>{await updateDoc(doc(db,'produtos',id),{ativo:!ativo,estadoAprovacao:!ativo?'aprovado':'oculto',atualizadoEm:serverTimestamp()});carregarProdutos()};window.pagar=async(id,vendedor,plano)=>{const d=new Date();plano==='semanal'?d.setDate(d.getDate()+7):d.setMonth(d.getMonth()+1);await updateDoc(doc(db,'pedidosSubscricao',id),{estado:'pago',pagoEm:serverTimestamp()});await updateDoc(doc(db,'usuarios',vendedor),{subscricaoAtiva:true,planoSubscricao:plano,proximaCobrancaEm:Timestamp.fromDate(d),estadoConta:'ativo',atualizadoEm:serverTimestamp()});await carregarPedidosSubscricao();await carregarVendedores();alert('Pagamento confirmado e vendedor ativado.')};function esc(v){return String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));}
