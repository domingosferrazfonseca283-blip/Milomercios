import { auth, db } from './firebase-config.js';
import { signInWithEmailAndPassword } from 'https://www.gstatic.com/firebasejs/10.8.1/firebase-auth.js';
import { doc, getDoc, setDoc, serverTimestamp } from 'https://www.gstatic.com/firebasejs/10.8.1/firebase-firestore.js';
const ADMIN_EMAIL='domingosferrazfonseca283@gmail.com';
document.getElementById('btn-login').addEventListener('click',async()=>{const email=document.getElementById('log-email').value.trim(),senha=document.getElementById('log-senha').value;if(!email||!senha)return alert('Preencha todos os campos.');try{const cred=await signInWithEmailAndPassword(auth,email,senha);const ref=doc(db,'usuarios',cred.user.uid);const snap=await getDoc(ref);let p=snap.exists()?snap.data():{};if((cred.user.email||'').toLowerCase()===ADMIN_EMAIL){if(!cred.user.emailVerified)return alert('Confirme primeiro o e-mail do administrador.');if(p.tipo!=='admin'||p.email!==ADMIN_EMAIL){await setDoc(ref,{email:ADMIN_EMAIL,tipo:'admin',nome:'Administrador Milomércios',estadoConta:'ativo',subscricaoAtiva:false,atualizadoEm:serverTimestamp()},{merge:true});p={...p,tipo:'admin',email:ADMIN_EMAIL}}}location.href=p.tipo==='admin'&&email.toLowerCase()===ADMIN_EMAIL?'admin.html':p.tipo==='vendedor'?'vendedor.html':'cliente.html';}catch(e){alert(e.code==='auth/invalid-credential'?'E-mail ou senha incorretos.':'Erro ao entrar: '+e.message)}});
